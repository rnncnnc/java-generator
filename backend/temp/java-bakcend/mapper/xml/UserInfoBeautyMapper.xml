<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace 需要映射的接口的全类名 -->
<mapper namespace="com.qinge.bakcend.mapper.UserInfoBeautyMapper">
	<sql id="UserInfoBeauty">
		id,
		email,
		status,
		user_id,
	</sql>

	<!-- 	批量查询数据	 -->
	<select id="select" resultType="com.qinge.bakcend.entity.pojo.UserInfoBeautyPojo">
		select
		<trim suffixOverrides=",">
			<include refid="UserInfoBeauty">
			</include>
		</trim>
		from user_info_beauty
		<if test="pojo != null">
			<where>
				<if test="pojo.id != null">
					and id = #{pojo.id}
				</if>
			</where>
		</if>
		<if test="page != null">
			limit #{page.pageSize} offset #{page.pageNum}
		</if>
		<if test="sort != null">
			order by #{sort.index} #{sort.order}
		</if>
	</select>

	<!-- 	插入数据	 -->
	<insert id="insert" parameterType="com.qinge.bakcend.entity.pojo.UserInfoBeautyPojo">
		insert into user_info_beauty
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="pojo.id != null">
				id,
			</if>
			<if test="pojo.email != null">
				email,
			</if>
			<if test="pojo.status != null">
				status,
			</if>
			<if test="pojo.userId != null">
				user_id,
			</if>
		</trim>
		values
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="pojo.id != null">
				#{pojo.id},
			</if>
			<if test="pojo.email != null">
				#{pojo.email},
			</if>
			<if test="pojo.status != null">
				#{pojo.status},
			</if>
			<if test="pojo.userId != null">
				#{pojo.userId},
			</if>
		</trim>
	</insert>

	<!-- 	更新数据	 -->
	<update id="update" parameterType="com.qinge.bakcend.entity.pojo.UserInfoBeautyPojo">
		update user_info_beauty
		<set>
			<if test="np.id != null">
				id = #{np.id},
			</if>
			<if test="np.email != null">
				email = #{np.email},
			</if>
			<if test="np.status != null">
				status = #{np.status},
			</if>
			<if test="np.userId != null">
				user_id = #{np.userId},
			</if>
		</set>
		<where>
			<if test="query.pojo.id != null">
				and id = #{query.pojo.id}
			</if>
		</where>
	</update>

	<!-- 	删除数据	 -->
	<delete id="delete" parameterType="com.qinge.bakcend.entity.pojo.UserInfoBeautyPojo">
		delete from user_info_beauty
		<where>
			<if test="pojo.id != null">
				and id = #{pojo.id}
			</if>
		</where>
	</delete>
</mapper>
